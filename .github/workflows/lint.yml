name: Lint

on:
  push

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      # Checkout code
      - name: Checkout code
        uses: actions/checkout@v2

      # Install
      - name: Install eslint
        run: sudo npm i -g eslint

      # Run
      - name: Run lint
        run: npm run lint